from django.apps import AppConfig


class AgendaBaseConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'agenda_base'
    verbose_name = 'Agenda Base (Models Abstratos)'
